{
  "version": "2.0.0",
  "tasks": [
  {
    "label": "build",
    "type": "shell",
    "command": "clear; BUILD_DIR='../build'; mkdir -p $BUILD_DIR; cmake -S ./ -B$BUILD_DIR; make -C $BUILD_DIR;",
    "problemMatcher": [
      "$gcc"
    ],
    "group": {
      "kind": "build",
      "isDefault": true
      }
    }
  ]
}
